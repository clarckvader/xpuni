VITE_API_BASE_URL=http://localhost:3000
# Note: the frontend client will append `/api` to this base URL, so set this to the server origin (e.g. http://localhost:3000)
VITE_STELLAR_EXPLORER_URL=https://soroban-testnet.stellar.expert
